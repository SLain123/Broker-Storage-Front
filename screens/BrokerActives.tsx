import { Text, SafeAreaView } from 'react-native';

const BrokerActives = () => {
    return (
        <SafeAreaView>
            <Text style={{ color: 'white' }}>First</Text>
        </SafeAreaView>
    );
};

export { BrokerActives };
